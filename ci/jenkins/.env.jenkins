# ============================================
# Jenkins Configuration
# ============================================
# Copiar este archivo y configurar tus credenciales

# Docker Hub Credentials (REQUERIDO para push de imágenes)
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_PASSWORD=your-dockerhub-password

# Jenkins Admin (Opcional - cambiar password por defecto)
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=admin123

# Configuración de Jenkins
JENKINS_OPTS=--httpPort=8080
JAVA_OPTS=-Djenkins.install.runSetupWizard=false -Djenkins.install.skipSetupWizard=true

# Repositorio Git (Opcional - si no usas el montaje local)
# GIT_REPO_URL=https://github.com/your-org/sports-athletics-module.git
# GIT_CREDENTIALS_ID=github-credentials
